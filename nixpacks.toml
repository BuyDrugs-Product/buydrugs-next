[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "cd buydrugs-next",
  "npm ci"
]

[phases.build]
cmds = [
  "cd buydrugs-next",
  "npm run build"
]

[start]
cmd = "cd buydrugs-next && npm run start"
